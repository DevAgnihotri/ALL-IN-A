rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Users can read/write their own user document
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Users can read/write their own profile document
    match /userProfiles/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Users can read/write their own health metrics
    match /healthMetrics/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Booking system rules - FIXED COLLECTION NAME
    match /bookings/{bookingId} {
      allow read, write: if request.auth != null;
    }
    
    // Booking slots rules - FIXED COLLECTION NAME (bookingSlots not booking-slots)
    match /bookingSlots/{slotId} {
      allow read, write: if request.auth != null;
    }
    
    // Personalization data
    match /personalization/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
